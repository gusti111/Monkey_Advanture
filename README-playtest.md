Playtest server

Run a local static server to play the game in your browser:

```bash
npm run start
# or specify PORT env e.g. PORT=9000 npm run start
```

Then open http://localhost:8080 in your browser.

This server is dependency-free and intended for quick local testing only.
